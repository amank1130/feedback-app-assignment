version: "3.8"

services:
  web:
    build: .
    container_name: feedback-app
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - SECRET_KEY=${SECRET_KEY}
    restart: unless-stopped
